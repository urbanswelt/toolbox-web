[project]
name = "toolbox-web"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = [
    "flask>=3.1.3",
    "huggingface-hub>=1.21.0",
    "python-dotenv>=1.2.2",
    "pyyaml>=6.0.3",
    "ruamel-yaml>=0.19.1",
]

[dependency-groups]
dev = [
    "ruff>=0.15.20",
]
